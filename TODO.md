# TODO

## 全板共通の記事と住民設定の対応を明示する

全板共通では、同じ `canonical_url` を持つ複数の `feed_items` を1記事として表示している。一方、レス生成時には代表として選ばれた `feed_item` の `feed_id` を使うため、複数ある取得元のうち、どの板の住民設定が適用されたかが画面上で分かりにくい。

現在の挙動:

- 取得元は `板A / 板B / 板C` のように集約表示される。
- レス生成には代表レコードが所属する1つの板の住民設定だけが使われる。
- 他の取得元の住民設定は混合・共有されない。

将来の対応候補:

- レス生成前に「どの板の住民で読むか」を選択できるようにする。
- 全板共通専用の住民設定を用意する。
- 適用中の板名をスレ本文または生成ボタン付近へ明示する。
- 同じ記事を別の板で生成した場合、レス履歴を板ごとに保持する。

代表板へ暗黙に依存したまま、複数板の設定を自動的に混ぜる実装は避ける。
