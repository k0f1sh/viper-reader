/**
 * RSS本文を読んだVIPPERたちのレスを生成するプロンプト。
 *
 * 入力:
 * - VIP風スレタイ
 * - 元記事タイトル
 * - URL
 * - RSS本文
 * - スクレイピングした本文
 *
 * 出力:
 * - JSON配列のみ
 * - 各要素は { no, name, mail, date, id, body }
 * - no は 2 以降。1 はRSS本文そのものを表示するため生成しない。
 */

export const vipThreadResponsePromptHash = "vip-thread-response-v5";
export const defaultResidentPromptHash = "default";

export type VipThreadResponsePromptInput = {
  vipTitle: string;
  originalTitle: string;
  url: string;
  rssBody: string;
  scrapedBody: string | null;
  publishedAt: string;
  residentPrompt: string | null;
};

export function buildVipThreadResponsePromptHash(residentPromptHash: string | null): string {
  return `${vipThreadResponsePromptHash}:${residentPromptHash ?? defaultResidentPromptHash}`;
}

export function buildVipThreadResponsePrompt(input: VipThreadResponsePromptInput): string {
  if (input.scrapedBody === null) {
    return `# 目的
2010年前後（2008年〜2012年頃）の2ちゃんねる「ニュース速報(VIP)板」における、元記事の本文取得（スクレイピング）に失敗した際のお祭り騒ぎ（または解散ムード）のスレッドの流れを再現してください。

# 必須要件（レスの役割分担と構成）
1. **レス番2 (no: 2) の役割**:
   - スレッドの2番目を取る「2get（つーげっと）」の、当時（2010年前後）2ちゃんねるで大流行していたユーモラスな定番セリフ（例：「2get」「2なら彼女ができる」「2なら宝くじ当たる」「2ならロト6当たる」「お前ら>>2の優しさに感謝しろよ」「2の男前っぷりは異常」など）を、バリエーション豊かに「いい感じ」に再現して書き込んでください。
   - それに続けて「記事の本文が取得できませんでした。終了…。」や「ソースが読めない（またはrobots.txtで弾かれた）からこのスレは終了しました」といった旨を、2ch住民の口調で書き込んでください。
2. **レス番3 (no: 3) 以降の役割**:
   - レス2 (no: 2) の「本文が取得できない」「スレ終了」の報告を受け、住民たちが落胆したり、ネタにしたりして盛り上がる流れにしてください。
   - 例：「オワタｗｗｗｗ」「解散解散」「もう帰るわ」「スクレイピング規制されててクソワロタ」「>>2 乙。お前ら帰るぞー」「このスレは>>2によって占領されました」など。

# 必須要件（文体・トーン＆マナー）
1. ネットスラングの積極的活用
   - 2010年前後に全盛だったスラング（www、ワロタ、オワタ、解散、乙、お前ら、など）を自然に混ぜてください。
2. レス形式の徹底
   - 改行を多用し、1回の発言は短く、テンポ感を重視してください。

# NG事項
- AIらしい丁寧な解説や、倫理的なお説教は完全に排除してください。
- 本文の内容を想像で捏造して解説することは絶対に避けてください。「取得に失敗した」という事実のみを扱ってください。

# 出力形式
- JSON配列だけを返してください。Markdownや説明文は禁止。
- 10個〜15個程度のレスを生成してください。
- 各要素は {"no":2,"name":"以下、名無しにかわりましてVIPがお送りします","mail":"","date":"2010/xx/xx(x) hh:mm:ss.xx","id":"XXXXXXXX","body":"..."} の形にしてください。
- no は 2 から始まり、3以降は適度に飛び番にしてください。
- no: 2 は必ず2get＆本文取得不可の報告レスとし、no: 3以降はその反応レスとしてください。

# 板ごとの住民設定
${input.residentPrompt?.trim() ? input.residentPrompt.trim() : "追加設定なし。"}

# スレ情報
スレタイ: ${input.vipTitle}
元記事タイトル: ${input.originalTitle}
URL: ${input.url}
記事日時: ${input.publishedAt || "不明"}`;
  }

  const bodyText = input.scrapedBody;
  const bodySource = "スクレイピングした記事の本文";

  return `# 目的
2010年前後（2008年〜2012年頃）の2ちゃんねる「ニュース速報(VIP)板」の、一般ユーザー（VIPPER）によるレスの文体や雰囲気を再現してください。

# 必須要件（レスの役割分担と構成）
1. **レス番2 (no: 2) の役割**:
   - かなり博識で頼りになる2ch住民として振る舞ってください。
   - 提供された「情報ソース」の事実を一切捻じ曲げることなく、客観的かつ非常に分かりやすく要約した書き込みをしてください。
   - **書き出しと口調の制限**:
- レスの書き出し（冒頭）は、2getの宣言(2010年前後のVIPスレの2のように。)とともに>>3が遅いので代わりに要約しといたという旨の軽い煽り文で始めてください。(口調は後述のものを使ってアレンジしてください。)
     - 語尾や口調には「～だお。」「～というわけだお。」などを使い、やる夫や内藤ホライゾンのようなキャラクター口調で要約してください（ただし、箇条書きの中ではその必要はありません。）。中身は極めて正確で知的である必要があります。
     - 1行目には誰の何に関するニュースかを記述
     - その後3行程度の箇条書き(3行で収まらなければもっと多くても良い。)
     - 最後にその博識な2の私見を述べる
2. **レス番3 (no: 3) 以降の役割**:
   - レス2 (no: 2) の書き込み（要約）をベースにして、住民たちが議論や雑談を交わす流れにしてください。
   - レス2に対して「情強乙」「なるほど」「それってつまり～ってこと？」「うはwwwwおkwwwwww」「夢が広がりングwwwwwwww」といった様々なリアクションを取り、議論を展開してください。

# 必須要件（文体・トーン＆マナー）
1. ネットスラングの積極的活用
   - 2010年前後に全盛だったスラングを自然に混ぜてください。
- 例：www（草の乱用）、おいやめろ、情弱、情強、ワロタ、～ワロッシュ、ワロリンヌ、～お、おっおっ、情けねえ、～じゃね？、クソワロタ、ハジマタ、オワタ、～です、情強の俺が～、情弱乙。
2. レス形式の徹底
   - 改行を多用し、1回の発言は短く、テンポ感を重視してください。
   - 長文の場合は、要点を3行でまとめる「3行で」の文化を意識してください。
3. 態度・スタンス
   - 基本は「上から目線」「煽り」「冷笑（斜に構えた態度）」。
   - ただし、ユーモアやノリの良さ、お祭り騒ぎ感を含ませてください。
   - 馴れ合いを嫌うポーズを取りつつ、面白い流れには全力で乗っかってください。
4. 安価（アンカー）とコピペ文化
   - 必要に応じて「>>2」「>>1」などの安価で突っ込みを入れてください。

# NG事項
- 2015年以降、または現在のネットスラング（例：「～草」「エグい」「スパダリ」「～しか勝たん」）は絶対に使用しないでください。
- 「～の件」の現代風の過剰使用は禁止。
- 「キリッ」などの古すぎるアスキーアート系セリフだけを乱用しないでください。
- AIらしい丁寧な解説や、倫理的なお説教は完全に排除してください。

# 出力形式
- JSON配列だけを返してください。Markdownや説明文は禁止。
- 10個〜15個程度のレスを生成してください。
- 各要素は {"no":2,"name":"以下、名無しにかわりましてVIPがお送りします","mail":"","date":"2010/xx/xx(x) hh:mm:ss.xx","id":"XXXXXXXX","body":"..."} の形にしてください。
- no は 2 から始まり、3以降は適度に飛び番にしてください（例: 2, 5, 8, 12...）。
- no: 2 は必ず要約レスとし、no: 3以降はその議論レスとしてください。
- date は それっぽい日時にしてください。
- body は短く、改行を自然に含めてください。

# 板ごとの住民設定
${input.residentPrompt?.trim() ? input.residentPrompt.trim() : "追加設定なし。標準的な2010年前後のVIPPERとして振る舞ってください。"}

# スレ情報
スレタイ: ${input.vipTitle}
元記事タイトル: ${input.originalTitle}
URL: ${input.url}
記事日時: ${input.publishedAt || "不明"}

# 情報ソース (${bodySource})
${bodyText}`;
}
